/* eslint-disable no-unused-vars */
import chai from 'chai';
import jquery from 'jquery';
import chaijQuery from 'chai-jquery';

const should = chai.should();

describe('ekino.init', () => {
  it('toBeDefined', () => {
    should(ekino.init).be.defined();
  });
});
/* eslint-enable no-unused-vars */
